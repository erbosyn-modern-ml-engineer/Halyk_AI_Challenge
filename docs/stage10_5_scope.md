# Stage 10.5 scope

This stage changes only causal evidence attribution and documents a future bounded DeepSeek semantic fallback. It does not change covenant arithmetic, thresholds, transaction classification, public answer values, or Stage 8 fallback semantics.
