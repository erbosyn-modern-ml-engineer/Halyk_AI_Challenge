[project]
name = "halyk-agent"
version = "0.1.0"
description = "Evidence-first decision agent for the Halyk Bank Agentic Challenge"
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.12,<3.13"
authors = [
  { name = "Halyk Agentic Challenge Team" },
]
dependencies = [
  "charset-normalizer>=3.3.0",
  "fastapi>=0.115.0",
  "openpyxl>=3.1.0",
  "pydantic>=2.9.0",
  "pydantic-settings>=2.5.0",
  "pypdf>=6.0.0",
  "uvicorn[standard]>=0.30.0",
]

[project.scripts]
halyk-agent = "halyk_agent.app.cli:main"

[project.optional-dependencies]
full = [
    "docling==2.118.0",
]
retrieval-fast = [
    "numpy>=1.26.0",
    "sentence-transformers>=3.0.0",
]
retrieval-full = [
    "alembic>=1.13.0",
    "asyncpg>=0.29.0",
    "numpy>=1.26.0",
    "pgvector>=0.3.0",
    "sentence-transformers>=3.0.0",
    "sqlalchemy[asyncio]>=2.0.0",
]

[dependency-groups]
dev = [
  "httpx>=0.27.0",
  "mypy>=1.11.0",
  "pytest>=8.3.0",
  "pytest-asyncio>=0.24.0",
  "reportlab>=4.0.0",
  "ruff>=0.6.0",
  "types-openpyxl>=3.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/halyk_agent"]

[tool.uv]
package = true
default-groups = ["dev"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]
pythonpath = ["src", "."]
markers = [
  "docling: tests that require the optional Docling full extra",
  "postgres: tests that require PostgreSQL (HALYK_POSTGRES_DSN; never start Docker)",
  "retrieval_models: tests that resolve pinned embedding/reranker model-lock entries",
]

[tool.ruff]
target-version = "py312"
line-length = 100
src = ["src", "tests"]
exclude = [
  ".venv",
  "__MACOSX",
  "agentic-bank-public",
  "work",
]

[tool.ruff.lint]
select = [
  "E",
  "F",
  "I",
  "B",
  "UP",
  "SIM",
  "RUF",
]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["S101"]

[tool.mypy]
python_version = "3.12"
strict = true
warn_return_any = true
warn_unused_configs = true
mypy_path = "src"
packages = ["halyk_agent"]
exclude = ["tests/"]

[[tool.mypy.overrides]]
module = [
  "docling",
  "docling.*",
  "sentence_transformers",
  "sentence_transformers.*",
  "numpy",
  "numpy.*",
  "torch",
  "torch.*",
]
ignore_missing_imports = true
follow_imports = "skip"

# When installed, use real SQLAlchemy/pgvector types (do not follow_imports=skip).
[[tool.mypy.overrides]]
module = [
  "asyncpg",
  "asyncpg.*",
  "pgvector",
  "pgvector.*",
  "alembic",
  "alembic.*",
  "sqlalchemy",
  "sqlalchemy.*",
]
ignore_missing_imports = true

